---
phase: 06-verification-and-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REPLAY-02, HEAT-02, ERR-02, WEB-02]

must_haves:
  truths:
    - "Session recordings are playable in PostHog Replay tab"
    - "Heatmap overlay is visible on Botmem pages via PostHog toolbar"
    - "Errors with stack traces appear in PostHog Error Tracking view"
    - "Navigation paths between pages are trackable in PostHog"
  artifacts: []
  key_links:
    - from: "Botmem web app"
      to: "PostHog Replay tab"
      via: "posthog-js session recording"
      pattern: "session_recording"
    - from: "Botmem web app"
      to: "PostHog Error Tracking"
      via: "capture_exceptions + PostHogExceptionFilter"
      pattern: "capture_exceptions"
---

<objective>
Verify that all PostHog data collection features enabled in Phase 5 are actively flowing data into PostHog cloud.

Purpose: Confirm session replay, heatmaps, error tracking, and navigation path data are visible in PostHog before building dashboards on top of them.
Output: Verified data flows -- no code artifacts (Phase 5 already wrote all the code).
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-sdk-feature-enablement/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate PostHog data by browsing Botmem</name>
  <files></files>
  <action>
Browse the Botmem web app at http://localhost:5173 to generate PostHog telemetry data. Perform these actions using the browser agent:

1. Navigate to the Dashboard page (/)
2. Navigate to the Connectors page (/connectors)
3. Navigate to the Memory Explorer page (/memory)
4. Type a search query in the memory search bar (generates autocapture + input events)
5. Click on several UI elements (buttons, links, cards) to generate click heatmap data
6. Deliberately trigger a frontend JS error by opening the browser console and running: `throw new Error('Phase 6 verification test error')`
7. Trigger a backend 500 error by calling a non-existent internal endpoint or similar (to test PostHogExceptionFilter capture)

Wait 30 seconds after all actions to allow PostHog to flush events.

This generates the session replay recording, heatmap click data, autocapture events, error captures, and page navigation path data that the next task will verify.
  </action>
  <verify>
    <automated>curl -s http://localhost:5173 | head -5</automated>
  </verify>
  <done>Botmem app has been browsed with multiple page navigations, clicks, search input, and deliberate errors triggered. PostHog should now have data to verify.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify data flows in PostHog dashboard</name>
  <files></files>
  <action>
User verifies PostHog data flows in the PostHog cloud dashboard. No code changes needed -- this is a human verification of data flowing from Phase 5 SDK features.
  </action>
  <how-to-verify>
Open PostHog at https://eu.i.posthog.com and verify each data flow:

1. **Session Replay (REPLAY-02):** Go to Replay tab. Confirm at least one session recording exists and is playable. Text inputs should appear masked.

2. **Heatmaps (HEAT-02):** Go to any Botmem page (e.g. http://localhost:5173/memory), enable the PostHog toolbar (PostHog settings -> Toolbar -> Launch), and verify the heatmap overlay shows click data on page elements.

3. **Error Tracking (ERR-02):** Go to Error Tracking view. Confirm the "Phase 6 verification test error" (frontend) and any backend 5xx error appear with stack traces.

4. **Navigation Paths (WEB-02):** Go to Web Analytics or create a Paths insight in PostHog. Verify that page navigation events show paths between Dashboard, Connectors, and Memory Explorer pages.
  </how-to-verify>
  <verify>User confirms all 4 data flows are visible in PostHog dashboard</verify>
  <done>Session replay playable, heatmap overlay visible, errors with stack traces in Error Tracking, and navigation paths trackable in PostHog.</done>
  <resume-signal>Type "verified" if all 4 data flows confirmed, or describe which ones are missing/broken</resume-signal>
</task>

</tasks>

<verification>
All four data flow categories (session replay, heatmaps, error tracking, navigation paths) confirmed working in PostHog cloud dashboard.
</verification>

<success_criteria>
- At least one session recording is playable in PostHog Replay tab
- Heatmap overlay shows click data on Botmem pages via PostHog toolbar
- Frontend and backend errors appear in PostHog Error Tracking with stack traces
- Page navigation paths are visible in PostHog (Web Analytics or Paths insight)
</success_criteria>

<output>
After completion, create `.planning/phases/06-verification-and-dashboards/06-01-SUMMARY.md`
</output>
