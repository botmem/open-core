---
phase: 18-api-keys
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - apps/web/src/pages/SettingsPage.tsx
  - apps/web/src/components/settings/ApiKeysTab.tsx
  - apps/web/src/components/settings/CreateKeyModal.tsx
  - apps/web/src/components/settings/KeyCreatedModal.tsx
  - apps/web/src/hooks/useApiKeys.ts
  - apps/web/src/lib/api.ts
autonomous: false
requirements: [KEY-01, KEY-04]

must_haves:
  truths:
    - "User can see a list of their API keys with name, masked key (last 4 chars), created date, and expiry"
    - "User can create a new API key by entering a name and optional expiry date"
    - "After creation, user sees the full key exactly once with a copy button and warning"
    - "User can revoke a key via confirmation dialog and it disappears from the list"
    - "Key count shows against limit (e.g. 3/10)"
  artifacts:
    - path: "apps/web/src/pages/SettingsPage.tsx"
      provides: "Tabbed settings page with Profile, API Keys, Pipeline sections"
    - path: "apps/web/src/components/settings/ApiKeysTab.tsx"
      provides: "Key list + create button + revoke flow"
      min_lines: 50
    - path: "apps/web/src/components/settings/CreateKeyModal.tsx"
      provides: "Modal with name input + optional expiry date"
    - path: "apps/web/src/components/settings/KeyCreatedModal.tsx"
      provides: "Modal showing raw key once with copy button + warning"
    - path: "apps/web/src/hooks/useApiKeys.ts"
      provides: "CRUD operations for API keys"
      exports: ["useApiKeys"]
    - path: "apps/web/src/lib/api.ts"
      provides: "API client methods for key endpoints"
  key_links:
    - from: "apps/web/src/components/settings/ApiKeysTab.tsx"
      to: "apps/web/src/hooks/useApiKeys.ts"
      via: "hook call for data + mutations"
      pattern: "useApiKeys"
    - from: "apps/web/src/hooks/useApiKeys.ts"
      to: "apps/web/src/lib/api.ts"
      via: "api.createApiKey, api.listApiKeys, api.revokeApiKey"
      pattern: "api\\.(create|list|revoke)ApiKey"
    - from: "apps/web/src/pages/SettingsPage.tsx"
      to: "apps/web/src/components/settings/ApiKeysTab.tsx"
      via: "Tab content rendering"
      pattern: "ApiKeysTab"
---

<objective>
Frontend Settings page with API Keys management tab

Purpose: Give users a UI to create, view, and revoke API keys. Restructures the existing Settings page into a tabbed layout (Profile, API Keys, Pipeline) per user decision.
Output: Working API Keys tab in Settings with create modal, key-shown-once modal, and revoke flow.
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-api-keys/18-CONTEXT.md
@.planning/phases/18-api-keys/18-01-SUMMARY.md

<interfaces>
<!-- API endpoints created in Plan 01 that this plan consumes -->

POST /api/api-keys
  Request: { name: string, expiresAt?: string }
  Response: { key: 'bm_sk_...', id: string, name: string, lastFour: string }

GET /api/api-keys
  Response: Array<{ id: string, name: string, lastFour: string, createdAt: string, expiresAt: string | null, revokedAt: string | null }>

DELETE /api/api-keys/:id
  Response: { success: true }

<!-- Existing UI components to reuse -->
From apps/web/src/components/ui/Modal.tsx -- existing modal component
From apps/web/src/components/ui/Tabs.tsx -- existing tabs component
From apps/web/src/components/ui/Card.tsx -- existing card component
From apps/web/src/components/ui/Button.tsx -- existing button component
From apps/web/src/components/ui/Input.tsx -- existing input component
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client methods, useApiKeys hook, and Settings page tab restructure</name>
  <files>
    apps/web/src/lib/api.ts,
    apps/web/src/hooks/useApiKeys.ts,
    apps/web/src/pages/SettingsPage.tsx,
    apps/web/src/components/settings/ApiKeysTab.tsx,
    apps/web/src/components/settings/CreateKeyModal.tsx,
    apps/web/src/components/settings/KeyCreatedModal.tsx
  </files>
  <action>
    1. Add API client methods to `apps/web/src/lib/api.ts`:
       - `createApiKey(name: string, expiresAt?: string)` -- POST /api-keys
       - `listApiKeys()` -- GET /api-keys
       - `revokeApiKey(id: string)` -- DELETE /api-keys/:id

    2. Create `apps/web/src/hooks/useApiKeys.ts`:
       - State: keys array, loading, error
       - `fetchKeys()` -- load on mount via api.listApiKeys()
       - `createKey(name, expiresAt?)` -- calls api.createApiKey, returns the raw key for display, then refetches list
       - `revokeKey(id)` -- calls api.revokeApiKey, refetches list
       - Returns { keys, loading, error, createKey, revokeKey }

    3. Create `apps/web/src/components/settings/CreateKeyModal.tsx`:
       - Props: isOpen, onClose, onCreate(name, expiresAt?)
       - Name input (required), optional expiry date input (type="date")
       - Create button, disabled when name is empty
       - Shows error if creation fails (e.g., duplicate name, limit reached)

    4. Create `apps/web/src/components/settings/KeyCreatedModal.tsx`:
       - Props: isOpen, onClose, apiKey (the raw key string)
       - Displays the full key in a monospace readonly input
       - Copy button that copies to clipboard (navigator.clipboard.writeText)
       - Warning text: "This key will only be shown once. Copy it now."
       - Close button (onClose)

    5. Create `apps/web/src/components/settings/ApiKeysTab.tsx`:
       - Uses useApiKeys() hook
       - Header showing count: "API KEYS (3/10)"
       - "CREATE KEY" button opens CreateKeyModal
       - After creation, shows KeyCreatedModal with the raw key
       - Key list: each row shows name, `bm_sk_...{lastFour}`, created date, expiry (or "Never"), Revoke button
       - Revoke button: inline confirmation ("Are you sure?" with Cancel/Confirm) or simple window.confirm
       - Empty state: "No API keys yet. Create one to use with the CLI or agents."
       - Loading state: skeleton rows

    6. Restructure `apps/web/src/pages/SettingsPage.tsx`:
       - Use the existing Tabs component to create tabs: Profile, API Keys, Pipeline
       - "Profile" tab contains the existing user profile card (name, email -- already there)
       - "API Keys" tab renders ApiKeysTab
       - "Pipeline" tab contains the existing pipeline concurrency settings + auto-enrich toggle + danger zone
       - Use URL search params (`?tab=api-keys`) to persist tab selection, default to "profile"
       - Existing functionality (concurrency settings, danger zone) must continue to work unchanged in the Pipeline tab
  </action>
  <verify>
    <automated>cd /Users/amr/Projects/botmem/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - Settings page has three tabs: Profile, API Keys, Pipeline
    - API Keys tab shows key list with name, masked key, dates
    - Create modal accepts name + optional expiry, returns key
    - Key-created modal shows raw key once with copy button and warning
    - Revoke confirms and removes key from list
    - Count displayed as "N/10"
    - Tab state persisted in URL search params
    - Existing pipeline settings still work in Pipeline tab
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify API key management UI and end-to-end auth flow</name>
  <files>apps/web/src/pages/SettingsPage.tsx</files>
  <action>Human verification of the complete API key management flow including UI and Bearer auth.</action>
  <verify>
    <automated>cd /Users/amr/Projects/botmem/apps/web && npx tsc --noEmit</automated>
  </verify>
  <done>User confirms all 14 verification steps pass</done>
  <what-built>
    API key management UI in the Settings page. Three tabs (Profile, API Keys, Pipeline). Create, view, copy, and revoke API keys. Also verifies that API key Bearer auth works for read endpoints and is rejected on write endpoints.
  </what-built>
  <how-to-verify>
    1. Navigate to Settings page (sidebar "SETTINGS" link)
    2. Verify three tabs appear: Profile, API Keys, Pipeline
    3. Click "API Keys" tab
    4. Verify empty state message shown
    5. Click "CREATE KEY" -- enter name "test-key-1", leave expiry empty, click Create
    6. Verify modal shows full key starting with bm_sk_ -- copy it
    7. Close the modal -- verify the key appears in the list with masked value
    8. Test the key works for reads: `curl -H "Authorization: Bearer bm_sk_YOURKEY" http://localhost:12412/api/memories`
    9. Test the key is blocked for writes: `curl -X POST -H "Authorization: Bearer bm_sk_YOURKEY" http://localhost:12412/api/memories/purge` -- should return 403
    10. Back in UI, click Revoke on the key, confirm
    11. Verify key disappears from list
    12. Test revoked key: `curl -H "Authorization: Bearer bm_sk_YOURKEY" http://localhost:12412/api/memories` -- should return 401
    13. Click "Pipeline" tab -- verify concurrency settings still work
    14. Click "Profile" tab -- verify name/email shown
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` -- no type errors
- Settings page loads with three tabs
- API key CRUD flow works end-to-end
- API key authenticates read endpoints, rejected on write endpoints
- Revoked key no longer authenticates
- Pipeline settings in Pipeline tab still save correctly
</verification>

<success_criteria>
- Settings page restructured into Profile / API Keys / Pipeline tabs
- User can create, view (masked), copy (once), and revoke API keys
- Key count shown against 10-key limit
- Tab state preserved in URL params
- End-to-end verification: create key -> use for read -> blocked on write -> revoke -> rejected
</success_criteria>

<output>
After completion, create `.planning/phases/18-api-keys/18-02-SUMMARY.md`
</output>
