---
phase: 18-api-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/db/schema.ts
  - apps/api/src/db/db.service.ts
  - apps/api/src/api-keys/api-keys.module.ts
  - apps/api/src/api-keys/api-keys.service.ts
  - apps/api/src/api-keys/api-keys.controller.ts
  - apps/api/src/api-keys/__tests__/api-keys.service.test.ts
  - apps/api/src/user-auth/jwt-auth.guard.ts
  - apps/api/src/user-auth/decorators/requires-jwt.decorator.ts
  - apps/api/src/app.module.ts
  - apps/api/src/accounts/accounts.controller.ts
  - apps/api/src/auth/auth.controller.ts
  - apps/api/src/memory/memory.controller.ts
  - apps/api/src/contacts/contacts.controller.ts
  - apps/api/src/jobs/jobs.controller.ts
  - apps/api/src/agent/agent.controller.ts
  - apps/api/src/settings/settings.controller.ts
autonomous: true
requirements: [KEY-01, KEY-02, KEY-03, KEY-04, KEY-05]

must_haves:
  truths:
    - "POST /api/api-keys with JWT creates a named API key and returns the raw key exactly once"
    - "GET /api/api-keys with JWT returns list of keys with masked data (name, lastFour, createdAt, expiresAt) -- never raw key or hash"
    - "DELETE /api/api-keys/:id with JWT revokes the key and it no longer authenticates"
    - "Authorization: Bearer bm_sk_... authenticates as the key owner with read-only scopes"
    - "API key requests to mutation endpoints (POST /jobs/sync, DELETE /memories, etc.) return 403"
    - "API key requests to read endpoints (GET /memories, POST /memories/search) succeed"
    - "Expired API keys return 401"
    - "Maximum 10 keys per user enforced"
  artifacts:
    - path: "apps/api/src/db/schema.ts"
      provides: "apiKeys table definition"
      contains: "apiKeys = sqliteTable"
    - path: "apps/api/src/api-keys/api-keys.service.ts"
      provides: "Key CRUD + validation logic"
      exports: ["ApiKeysService"]
    - path: "apps/api/src/api-keys/api-keys.controller.ts"
      provides: "REST endpoints for key management"
      exports: ["ApiKeysController"]
    - path: "apps/api/src/user-auth/jwt-auth.guard.ts"
      provides: "Dual auth guard (JWT + API key prefix detection)"
      exports: ["JwtAuthGuard"]
    - path: "apps/api/src/user-auth/decorators/requires-jwt.decorator.ts"
      provides: "Decorator to block API key access"
      exports: ["RequiresJwt", "REQUIRES_JWT_KEY"]
  key_links:
    - from: "apps/api/src/user-auth/jwt-auth.guard.ts"
      to: "apps/api/src/api-keys/api-keys.service.ts"
      via: "validateKey() call on bm_sk_ prefix"
      pattern: "apiKeysService\\.validateKey"
    - from: "apps/api/src/user-auth/jwt-auth.guard.ts"
      to: "apps/api/src/user-auth/decorators/requires-jwt.decorator.ts"
      via: "reflector check for REQUIRES_JWT_KEY metadata"
      pattern: "REQUIRES_JWT_KEY"
    - from: "apps/api/src/api-keys/api-keys.service.ts"
      to: "apps/api/src/db/schema.ts"
      via: "Drizzle queries on apiKeys table"
      pattern: "schema\\.apiKeys"
---

<objective>
API key backend: schema, service, controller, dual auth guard, and read-only enforcement

Purpose: Enable programmatic read-only access to Botmem via named API keys that coexist with JWT authentication. This is the complete backend for KEY-01 through KEY-05.
Output: Working API key CRUD endpoints, dual auth guard, and @RequiresJwt decorator applied to all mutation endpoints.
</objective>

<execution_context>
@/Users/amr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-api-keys/18-CONTEXT.md
@.planning/phases/18-api-keys/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/api/src/db/schema.ts:
```typescript
export const users = sqliteTable('users', {
  id: text('id').primaryKey(),
  email: text('email').notNull().unique(),
  passwordHash: text('password_hash').notNull(),
  name: text('name').notNull(),
  onboarded: integer('onboarded').notNull().default(0),
  createdAt: text('created_at').notNull(),
  updatedAt: text('updated_at').notNull(),
});

export const refreshTokens = sqliteTable('refresh_tokens', {
  id: text('id').primaryKey(),
  userId: text('user_id').notNull().references(() => users.id),
  tokenHash: text('token_hash').notNull(),
  family: text('family').notNull(),
  expiresAt: text('expires_at').notNull(),
  revokedAt: text('revoked_at'),
  createdAt: text('created_at').notNull(),
});
```

From apps/api/src/user-auth/jwt-auth.guard.ts:
```typescript
@Injectable()
export class JwtAuthGuard extends AuthGuard('jwt') {
  constructor(private reflector: Reflector) { super(); }
  canActivate(context: ExecutionContext) {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [...]);
    if (isPublic) return true;
    return super.canActivate(context);
  }
}
```

From apps/api/src/user-auth/decorators/public.decorator.ts:
```typescript
export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

From apps/api/src/app.module.ts:
```typescript
providers: [{ provide: APP_GUARD, useClass: JwtAuthGuard }]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema, service, controller, and module for API keys</name>
  <files>
    apps/api/src/db/schema.ts,
    apps/api/src/db/db.service.ts,
    apps/api/src/api-keys/api-keys.module.ts,
    apps/api/src/api-keys/api-keys.service.ts,
    apps/api/src/api-keys/api-keys.controller.ts,
    apps/api/src/api-keys/__tests__/api-keys.service.test.ts,
    apps/api/src/app.module.ts
  </files>
  <behavior>
    - generateKey returns { raw: 'bm_sk_...' (41 chars), hash: string (64 hex chars) }
    - create() stores SHA-256 hash, returns raw key + id + name + lastFour
    - create() throws when user already has 10 keys
    - create() throws when name is duplicate for same user
    - create() accepts optional expiresAt ISO string
    - listByUser() returns keys with id, name, lastFour, createdAt, expiresAt, revokedAt -- never raw key or hash
    - revoke() sets revokedAt timestamp; revoked key no longer validates
    - validateKey() returns key record for valid active key; null for invalid/revoked/expired
    - validateKey() returns null for expired keys (expiresAt in the past)
  </behavior>
  <action>
    1. Add `apiKeys` table to `apps/api/src/db/schema.ts` (follows refreshTokens pattern):
       - id (text PK), userId (text FK to users.id), name (text NOT NULL), keyHash (text NOT NULL), lastFour (text NOT NULL), bankIds (text nullable -- JSON array, null = all), expiresAt (text nullable), revokedAt (text nullable), createdAt (text NOT NULL)

    2. Add CREATE TABLE + indexes to `apps/api/src/db/db.service.ts` createTables():
       - `CREATE TABLE IF NOT EXISTS api_keys (...)` matching schema
       - `CREATE UNIQUE INDEX IF NOT EXISTS idx_api_keys_user_name ON api_keys(user_id, name)`
       - `CREATE INDEX IF NOT EXISTS idx_api_keys_hash ON api_keys(key_hash)`

    3. Create `apps/api/src/api-keys/api-keys.service.ts`:
       - Inject DbService
       - `generateKey()`: `bm_sk_` + `crypto.randomBytes(16).toString('hex')` = 41 char key; SHA-256 hash
       - `create(userId, name, expiresAt?)`: check count < 10, check name unique per user, generate key, insert row, return { key: raw, id, name, lastFour }
       - `listByUser(userId)`: select all non-revoked for user, return { id, name, lastFour, createdAt, expiresAt } -- exclude keyHash
       - `revoke(userId, keyId)`: set revokedAt = now. Verify key belongs to userId before revoking.
       - `validateKey(rawKey)`: SHA-256 hash the raw key, lookup by keyHash where revokedAt IS NULL, check expiresAt not past, return record or null

    4. Create `apps/api/src/api-keys/api-keys.controller.ts`:
       - `@Controller('api-keys')`, all three endpoints get `@RequiresJwt()` (from Task 2)
       - `POST /` -- create key. Body: { name: string, expiresAt?: string }. Uses @CurrentUser() for userId.
       - `GET /` -- list keys. Uses @CurrentUser() for userId.
       - `DELETE /:id` -- revoke key. Uses @CurrentUser() for userId.

    5. Create `apps/api/src/api-keys/api-keys.module.ts`:
       - `@Global()` module (so JwtAuthGuard can inject ApiKeysService without circular dep)
       - imports: [DbModule], providers: [ApiKeysService], exports: [ApiKeysService], controllers: [ApiKeysController]

    6. Register ApiKeysModule in `apps/api/src/app.module.ts` imports array.

    7. Create test file `apps/api/src/api-keys/__tests__/api-keys.service.test.ts` covering all behaviors listed above.
  </action>
  <verify>
    <automated>cd /Users/amr/Projects/botmem/apps/api && npx vitest run src/api-keys/__tests__/api-keys.service.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - apiKeys table created in schema.ts and db.service.ts
    - ApiKeysService generates bm_sk_ prefixed keys, stores SHA-256 hash, enforces 10-key limit and name uniqueness
    - ApiKeysController exposes POST/GET/DELETE /api-keys endpoints
    - ApiKeysModule registered as @Global() in AppModule
    - All service tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Dual auth guard, @RequiresJwt decorator, and mutation endpoint enforcement</name>
  <files>
    apps/api/src/user-auth/jwt-auth.guard.ts,
    apps/api/src/user-auth/decorators/requires-jwt.decorator.ts,
    apps/api/src/accounts/accounts.controller.ts,
    apps/api/src/auth/auth.controller.ts,
    apps/api/src/memory/memory.controller.ts,
    apps/api/src/contacts/contacts.controller.ts,
    apps/api/src/jobs/jobs.controller.ts,
    apps/api/src/agent/agent.controller.ts,
    apps/api/src/settings/settings.controller.ts,
    apps/api/src/api-keys/api-keys.controller.ts
  </files>
  <action>
    1. Create `apps/api/src/user-auth/decorators/requires-jwt.decorator.ts`:
       ```typescript
       import { SetMetadata } from '@nestjs/common';
       export const REQUIRES_JWT_KEY = 'requiresJwt';
       export const RequiresJwt = () => SetMetadata(REQUIRES_JWT_KEY, true);
       ```

    2. Modify `apps/api/src/user-auth/jwt-auth.guard.ts` for dual auth:
       - Add constructor params: `private reflector: Reflector` (existing), add `@Inject(forwardRef(() => ApiKeysService)) private apiKeysService: ApiKeysService`
       - Change `canActivate` to `async canActivate`
       - After @Public() check, extract Bearer token from `request.headers.authorization`
       - If token starts with `bm_sk_`: call `apiKeysService.validateKey(token)` (token = value after "Bearer ")
         - If null: throw UnauthorizedException
         - Check @RequiresJwt metadata: if true, throw ForbiddenException('This endpoint requires full authentication')
         - Set `request.user = { id: keyRecord.userId, apiKeyId: keyRecord.id, scopes: ['read'], bankIds: keyRecord.bankIds ? JSON.parse(keyRecord.bankIds) : null }`
         - Return true
       - Otherwise: delegate to `super.canActivate(context)` (existing JWT path)
       - Import ForbiddenException, UnauthorizedException from @nestjs/common; forwardRef, Inject from @nestjs/common
       - Import REQUIRES_JWT_KEY from requires-jwt.decorator

    3. Add `@RequiresJwt()` to ALL mutation endpoints across controllers. The complete list from RESEARCH.md:

       **accounts.controller.ts:** POST /, PATCH /:id, DELETE /:id
       **auth.controller.ts:** POST /:type/initiate, POST /:type/complete (note: these may already be @Public -- if so, skip since @Public takes precedence)
       **memory.controller.ts:** POST /retry-failed, POST /backfill-contacts, POST /backfill-embeddings, POST /relabel-unknown, POST /:id/pin, DELETE /:id/pin, POST /:id/recall, DELETE /:id, POST /purge, POST /vector-index/reset
       **contacts.controller.ts:** POST /auto-merge, POST /reclassify, PATCH /:id, DELETE /:id, DELETE /:id/identifiers/:identId, POST /:id/split, POST /:id/merge, POST /normalize, POST /suggestions/dismiss, POST /suggestions/undismiss
       **jobs.controller.ts:** POST /sync/:accountId, POST /retry-failed, DELETE /:id
       **agent.controller.ts:** POST /ask, POST /remember, DELETE /forget/:id, POST /summarize
       **settings.controller.ts:** PATCH / (updateSettings)
       **api-keys.controller.ts:** POST /, GET /, DELETE /:id (already has @RequiresJwt from Task 1)

       Do NOT add @RequiresJwt to these read endpoints (they accept both JWT and API key):
       - GET /memories, GET /memories/:id, GET /memories/stats, GET /memories/graph
       - POST /memories/search (POST but read-only)
       - GET /people, GET /people/:id
       - POST /people/search (POST but read-only)

    4. Verify the build compiles: `cd apps/api && npx tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/amr/Projects/botmem/apps/api && npx tsc --noEmit && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - JwtAuthGuard detects bm_sk_ prefix and routes to API key validation path
    - @RequiresJwt decorator exists and is applied to all mutation endpoints
    - API key auth on mutation endpoints returns 403 Forbidden
    - API key auth on read endpoints succeeds with reduced user context
    - JWT auth continues to work unchanged on all endpoints
    - TypeScript compiles without errors
    - Existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `cd apps/api && npx vitest run --reporter=verbose` -- all tests pass
- `cd apps/api && npx tsc --noEmit` -- no type errors
- Manual: `curl -X POST /api/user-auth/login` to get JWT, then `POST /api/api-keys` with JWT to create key, verify raw key returned
- Manual: `GET /api/api-keys` with JWT shows masked key list
- Manual: `GET /api/memories` with `Authorization: Bearer bm_sk_...` succeeds
- Manual: `POST /api/jobs/sync/xxx` with `Authorization: Bearer bm_sk_...` returns 403
- Manual: `DELETE /api/api-keys/:id` with JWT revokes key, subsequent Bearer auth with that key returns 401
</verification>

<success_criteria>
- API keys can be created, listed, and revoked via JWT-authenticated endpoints
- Raw key shown exactly once at creation, stored as SHA-256 hash
- bm_sk_ prefix on Bearer header routes to API key auth path
- API key auth on read endpoints works; mutation endpoints return 403
- 10 key limit enforced; duplicate names rejected
- Optional expiration works; expired keys rejected
- All existing tests pass alongside new ones
</success_criteria>

<output>
After completion, create `.planning/phases/18-api-keys/18-01-SUMMARY.md`
</output>
