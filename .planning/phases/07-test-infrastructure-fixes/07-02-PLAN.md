---
phase: 7
plan: 2
title: "Fix all failing tests"
wave: 2
depends_on: [1]
files_modified:
  - apps/api/src/accounts/__tests__/accounts.controller.test.ts
  - apps/api/src/auth/__tests__/auth.service.test.ts
  - apps/api/src/contacts/__tests__/contacts.service.test.ts
  - apps/api/src/db/__tests__/db.service.test.ts
  - apps/api/src/jobs/__tests__/jobs.controller.test.ts
  - apps/api/src/jobs/__tests__/sync.processor.test.ts
  - apps/api/src/logs/__tests__/logs.service.test.ts
  - apps/api/src/memory/__tests__/embed.processor.test.ts
  - apps/api/src/memory/__tests__/enrich.processor.test.ts
  - apps/api/src/memory/__tests__/memory.service.test.ts
  - apps/api/src/memory/__tests__/ollama.service.test.ts
  - apps/api/src/memory/__tests__/qdrant.service.test.ts
  - apps/api/src/memory/__tests__/resolveSlackContacts.test.ts
  - apps/web/src/components/__tests__/JobTable.test.tsx
  - apps/web/src/hooks/__tests__/useMemories.test.ts
  - apps/web/src/store/__tests__/memoryStore.test.ts
  - packages/connectors/imessage/src/__tests__/imessage.test.ts
  - packages/connectors/photos-immich/src/__tests__/immich.test.ts
  - packages/connectors/slack/src/__tests__/slack.test.ts
  - packages/connectors/slack/src/__tests__/sync.test.ts
  - packages/connectors/whatsapp/src/__tests__/whatsapp.test.ts
autonomous: true
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
must_haves:
  truths:
    - "pnpm test exits with 0 failed test files and 0 failed tests"
    - "No source/production files were modified — only test files changed"
  artifacts:
    - path: "apps/api/src/**/__tests__/*.test.ts"
      provides: "All 26 API test files passing"
    - path: "apps/web/src/**/__tests__/*.test.{ts,tsx}"
      provides: "All 13 web test files passing"
    - path: "packages/connectors/*/src/__tests__/*.test.ts"
      provides: "All connector test files passing"
---

# Plan 07-02: Fix all failing tests

## Goal
Fix all 21 failing test files (77 failing tests) so the entire test suite passes with zero failures. **Do NOT change any source/production code** — only fix test files to match current source code behavior.

## Context
- 21 test files failing across 3 areas: API (13 files), Web (3 files), Connectors (5 files)
- Tests likely fail because source code evolved but tests weren't updated
- Common failure patterns: changed function signatures, renamed exports, modified return types, updated schemas
- **CRITICAL CONSTRAINT: Only modify test files (*.test.ts, *.test.tsx). NEVER modify source/production code.**

## Tasks

<task id="1">
<files>
apps/api/src/accounts/__tests__/accounts.controller.test.ts
apps/api/src/auth/__tests__/auth.service.test.ts
apps/api/src/contacts/__tests__/contacts.service.test.ts
apps/api/src/db/__tests__/db.service.test.ts
apps/api/src/jobs/__tests__/jobs.controller.test.ts
apps/api/src/jobs/__tests__/sync.processor.test.ts
apps/api/src/logs/__tests__/logs.service.test.ts
</files>
<action>
Fix 7 core API service/controller test files. For each:
1. Read the corresponding source file to understand current behavior
2. Read the test file to identify what's wrong (imports, mocks, assertions)
3. Update the test to match current source code

Common fix patterns:
- Import path changes → update import statements
- Function signature changes → update mock implementations and call arguments
- New required dependencies → add mocked dependencies
- Schema changes → update test data objects to match current schema
</action>
<verify>
```bash
cd apps/api && npx vitest run src/accounts src/auth src/contacts src/db src/jobs src/logs 2>&1 | grep -E "FAIL|PASS|Tests:"
```
</verify>
<done>All 7 core API test files pass with zero failures</done>
</task>

<task id="2">
<files>
apps/api/src/memory/__tests__/embed.processor.test.ts
apps/api/src/memory/__tests__/enrich.processor.test.ts
apps/api/src/memory/__tests__/memory.service.test.ts
apps/api/src/memory/__tests__/ollama.service.test.ts
apps/api/src/memory/__tests__/qdrant.service.test.ts
apps/api/src/memory/__tests__/resolveSlackContacts.test.ts
</files>
<action>
Fix 6 memory module test files. For each:
1. Read the corresponding source file to understand current behavior
2. Read the test file to identify what's wrong
3. Update the test to match current source code

These files likely have more complex mocking (Ollama API, Qdrant client, BullMQ) — pay attention to mock shapes.
</action>
<verify>
```bash
cd apps/api && npx vitest run src/memory 2>&1 | grep -E "FAIL|PASS|Tests:"
```
</verify>
<done>All 6 memory module test files pass with zero failures</done>
</task>

<task id="3">
<files>
apps/web/src/components/__tests__/JobTable.test.tsx
apps/web/src/hooks/__tests__/useMemories.test.ts
apps/web/src/store/__tests__/memoryStore.test.ts
</files>
<action>
Fix 3 web test files. For each:
1. Read the corresponding source file
2. Read the test file
3. Update mocks, renders, and assertions to match current component/hook/store APIs
</action>
<verify>
```bash
cd apps/web && npx vitest run 2>&1 | grep -E "FAIL|PASS|Tests:"
```
</verify>
<done>All 3 web test files pass (and all other 10 web test files still pass)</done>
</task>

<task id="4">
<files>
packages/connectors/imessage/src/__tests__/imessage.test.ts
packages/connectors/photos-immich/src/__tests__/immich.test.ts
packages/connectors/slack/src/__tests__/slack.test.ts
packages/connectors/slack/src/__tests__/sync.test.ts
packages/connectors/whatsapp/src/__tests__/whatsapp.test.ts
</files>
<action>
Fix 5 connector test files. For each:
1. Read the connector source file
2. Read the test file
3. Update mocks and assertions to match current connector implementation
</action>
<verify>
```bash
for pkg in imessage photos-immich slack whatsapp; do echo "=== $pkg ===" && cd /Users/amr/Projects/botmem/packages/connectors/$pkg && npx vitest run 2>&1 | grep -E "FAIL|PASS|Tests:"; cd /Users/amr/Projects/botmem; done
```
</verify>
<done>All 5 connector test files pass with zero failures</done>
</task>

<task id="5">
<files>none (verification only)</files>
<action>
Run the full monorepo test suite to confirm zero failures:
```bash
pnpm test
```
If any tests still fail, iterate: read error → read source → fix test → re-run.
Do NOT stop until pnpm test shows 0 failures.
</action>
<verify>
```bash
pnpm test 2>&1 | tail -5
```
</verify>
<done>pnpm test shows 0 failed test files and 0 failed individual tests across all packages</done>
</task>

## Verification

- [ ] `pnpm test` shows 0 failed test files
- [ ] `pnpm test` shows 0 failed individual tests
- [ ] No source/production files were modified (only *.test.ts and *.test.tsx files changed)
- [ ] All 52 test files pass
