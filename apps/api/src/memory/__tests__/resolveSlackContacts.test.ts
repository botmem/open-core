import { describe, it, expect } from 'vitest';

describe('buildSlackIdentifiers (removed)', () => {
  it('was removed from source — slack identifier building is now inline in embed.processor', () => {
    // The buildSlackIdentifiers function was inlined into the embed processor
    // and is no longer exported. These tests are preserved as a placeholder.
    expect(true).toBe(true);
  });
});
