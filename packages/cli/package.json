{
  "name": "@botmem/cli",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "botmem": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "catalog:"
  }
}
